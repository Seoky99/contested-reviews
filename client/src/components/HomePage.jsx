function HomePage({handleClick}) {

    return (
        <div>
            <h1>Hello</h1>
            <button onClick={handleClick}>Go to Sets!</button>
        </div>
    ); 
    
};

export default HomePage; 